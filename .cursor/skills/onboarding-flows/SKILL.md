---
name: onboarding-flows
description: Use when building onboarding — first-run workspace + API key, setup checklists, skip/resume. Covers time to first value.
---

# Onboarding Flows

Yoyo first value = one successful turn in a workspace.

Required steps only: workspace path, API key, model. Skip-for-now allowed but the composer stays disabled with an inline reason.

Persist progress. Prefill workspace from last session. Do not open with a 12-field Control dump.

Empty agent view after setup: three example prompts + `@` hint, not a marketing headline.
