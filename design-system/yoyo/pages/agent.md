# Agent (thread)

Overrides MASTER for the transcript stage.

- Stage stays bare. Rail and review are inset cards. Composer is the brightest object.
- Empty thread is a still-life above the composer: MarkWell, one sentence, one hint line, three starter pills.
- Session identity lives on hover (rail card) and as a shortened chip in the title — never a debug dump in the brand row.
- Toolbar in the composer: ghost controls left, send circle right. No labelled buttons in icon clusters.
