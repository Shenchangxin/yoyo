Official protocol plugin sources and packed `.yingce-plugin` zips.

Packed at build/dev time from each subdirectory that contains `manifest.json`.
Payment plugins are not shipped. User-imported packages live in `~/.yoyo/video/plugins/user`.
