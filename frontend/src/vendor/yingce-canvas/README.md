# Vendored from open-ai-canvas

Source: `open-ai-canvas/web/src` at the commit pinned in `yoyo/UPSTREAM.md`.

Imports of `@/` were rewritten to `@yingce/`. HTTP goes through `services/api/request.ts` → `VideoCall("canvas.http")`. Live2D is stubbed. Do not treat this tree as the Yoyo design system.
